using System;

namespace AbianshKumanan
{
    public static class lab2
    {
        public static void Main()
        {
            //int grade
            //int passingCounter=0;


            //remember-average=sum/count

            //interger varible to hold the sum(accmukated grades)
            //doubleaccmulated grade=0;
            //double average=0;

            //for(int i=0; i<10; i++)
            //{
            //enter the students mark
            //grade= convert.ToInt32(console.ReadLine());
            //if grade greater than or equal 60 and less or equal too 100
            //if the grade is within the acceptable rang we increment 
            //passing counter++
            //accumulated grade = accumulated +grade
            // else they did not pass this cacluation
            //for loop done
            //average calcualtion (sum
            //}

            // how to validate if an input is a int in c sharp
            //stack overflow 


            int Grade;
            doubleaccmulated grade = 0;
            double average;

            for (int i = 0; i, 10; i++){
                Console.WriteLine("Please enter student mark")
  

                    grade = Convert.ToInt32(Console.ReadLine());
                if (grade >= 60 || grade <= 100)
                {

                    accmulated grade = accumulated + grade


                    }
                else
                {

                    Console.WriteLine("student is not in the passing range")



                    }
            }
            average = accumulatedgrade / grade;








        }
    }
}
